encrypt:
  key: ${TRANSFORMR_ENCRYPT_KEY}

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations:
            - file:///C:/Users/tamal/Documents/Workspace/transformr/configurations
  profiles:
    active:
      - native